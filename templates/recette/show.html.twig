{% extends 'base.html.twig' %}

{% block title %}Recette{% endblock %}

{% block body %}
    <h1>Recette</h1>

    {# <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recette.id }}</td>
            </tr>
            <tr>
                <th>Description3</th>
                <td>{{ recette.description3 }}</td>
            </tr>
            <tr>
                <th>Fiche</th>
                <td>{{ recette.fiche }}</td>
            </tr>
            <tr>
                <th>Avatar</th>
                <td>{{ recette.avatar }}</td>
            </tr>
        </tbody>
    </table> #}
    <div class= "d-flex justify-content-between"> <p>{{ recette.fiche }}</p>
    <p>Préparation:120 minutes</p></div>
    <div class= "d-flex justify-content-between">   <p>
                       {% for categorie in recette.categories %}
                       <a href="{{ path('app_categorie_index') }}">{{ categorie.description2 }}</a>
                       {% endfor %}
                    </p>
                    <p>Cuisson:120 minutes</p></div>
<div class= "d-flex justify-content-between">  <p>{{ recette.description3 }}</p> <p><ul>
                        {% for note in recette.notes %}
                        <li>{{ note.note }} / 5</li>
                        {% endfor %}
                    </ul></p></div>
<h1>Commentaires</h1>
<div class= "d-flex"> <ul>{% for note in recette.notes %}
<li>{{ note.description1 }}</li>
 {% endfor %}
</ul>

</div>
    <a href="{{ path('app_recette_index') }}">back to list</a>
{% if is_granted('ROLE_ADMIN') %}
    <a href="{{ path('app_recette_edit', {'id': recette.id}) }}">edit</a>

    {{ include('recette/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
